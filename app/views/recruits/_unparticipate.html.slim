= link_to 'changed my mind', recruit_participation_path(recruit_id: @recruit.id, id: current_user.participations.find_by(recruit_id: @recruit.id)), class: "delete_join-btn", method: :delete, remote: true 


/ <%= button_to tweet_like_path(like, tweet_id: tweet.id), method: :delete, id: "like-button", remote: true do %>
